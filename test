server:
  port: 8888

spring:
  application:
    name: config-server

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/polarbear-Yang/test.git
          searchPaths: micro-service-config
          username:
          password:
          
management:
  endpoints:
    web:
      exposure:
